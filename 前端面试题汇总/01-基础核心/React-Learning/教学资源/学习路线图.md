# React 学习路线图

> 本学习路线图专为想要系统学习 React 的开发者设计。从入门到精通，涵盖所有核心知识点，并配有实践建议和推荐资源。无论你是前端新手还是有经验的开发者，都能从中找到适合自己的学习路径。

---

## 目录

1. [学习路径总览](#1-学习路径总览)
2. [第一阶段：JavaScript 基础](#2-第一阶段javascript-基础)
3. [第二阶段：React 入门](#3-第二阶段react-入门)
4. [第三阶段：React 核心概念](#4-第三阶段react-核心概念)
5. [第四阶段：Hooks 深入](#5-第四阶段hooks-深入)
6. [第五阶段：状态管理](#6-第五阶段状态管理)
7. [第六阶段：路由与数据请求](#7-第六阶段路由与数据请求)
8. [第七阶段：性能优化](#8-第七阶段性能优化)
9. [第八阶段：TypeScript 与 React](#9-第八阶段typescript-与-react)
10. [第九阶段：服务端渲染与 Next.js](#9-第九阶段服务端渲染与-nextjs)
11. [第十阶段：测试与工程化](#10-第十阶段测试与工程化)
12. [实战项目建议](#11-实战项目建议)
13. [学习资源汇总](#12-学习资源汇总)

---

## 1. 学习路径总览

### 路线图概览

```
JavaScript 基础 (ES6+)
        ↓
    React 入门
        ↓
   组件与 Props
        ↓
   State 与生命周期
        ↓
    Hooks 基础
        ↓
    Hooks 进阶
        ↓
    状态管理
        ↓
    路由与数据请求
        ↓
    性能优化
        ↓
    TypeScript
        ↓
    服务端渲染
        ↓
    测试与工程化
```

### 学习时间建议

| 阶段 | 内容 | 建议时间 |
|------|------|----------|
| 第一阶段 | JavaScript 基础 | 1-2 周 |
| 第二阶段 | React 入门 | 1 周 |
| 第三阶段 | React 核心概念 | 1-2 周 |
| 第四阶段 | Hooks 深入 | 1-2 周 |
| 第五阶段 | 状态管理 | 1 周 |
| 第六阶段 | 路由与数据请求 | 1 周 |
| 第七阶段 | 性能优化 | 1 周 |
| 第八阶段 | TypeScript | 1 周 |
| 第九阶段 | 服务端渲染 | 1 周 |
| 第十阶段 | 测试与工程化 | 1 周 |

**总计：约 10-13 周**

---

## 2. 第一阶段：JavaScript 基础

> 在学习 React 之前，扎实的 JavaScript 基础是必不可少的。React 本质上是 JavaScript 的库，所以你需要熟悉现代 JavaScript（ES6+）的语法和概念。

### 学习目标

- 掌握 ES6+ 核心语法
- 理解异步编程（Promise、async/await）
- 熟悉模块化开发
- 了解 TypeScript 基础（可选）

### 核心知识点

#### 2.1 变量与作用域

```javascript
// let 和 const
let count = 0;       // 可变变量
const PI = 3.14159;  // 常量，不可重新赋值

// 块级作用域
{
  let blockScoped = '只在块内有效';
  var functionScoped = '整个函数可见';
}

// 闭包
function createCounter() {
  let count = 0;
  return {
    increment: () => ++count,
    getCount: () => count
  };
}
```

#### 2.2 函数与箭头函数

```javascript
// 传统函数
function add(a, b) {
  return a + b;
}

// 箭头函数
const add = (a, b) => a + b;

// 箭头函数与 this
function Timer() {
  this.time = 0;

  // 传统函数：this 指向调用者
  setInterval(function() {
    this.time++; // 问题：this 不指向 Timer
  }, 1000);

  // 箭头函数：捕获外层 this
  setInterval(() => {
    this.time++;
  }, 1000);
}
```

#### 2.3 解构与展开

```javascript
// 数组解构
const [first, second, ...rest] = [1, 2, 3, 4, 5];

// 对象解构
const { name, age } = { name: 'Alice', age: 25 };

// 函数参数解构
function greet({ name, age }) {
  return `Hello, ${name}, you are ${age}`;
}

// 展开运算符
const arr1 = [1, 2];
const arr2 = [...arr1, 3, 4]; // [1, 2, 3, 4]

const obj1 = { a: 1 };
const obj2 = { ...obj1, b: 2 }; // { a: 1, b: 2 }
```

#### 2.4 Promise 与 async/await

```javascript
// Promise
const fetchData = () => {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      resolve({ data: 'success' });
    }, 1000);
  });
};

// async/await
async function getData() {
  try {
    const response = await fetchData();
    console.log(response.data);
  } catch (error) {
    console.error(error);
  }
}

// 并行请求
const [users, posts] = await Promise.all([
  fetch('/api/users').then(r => r.json()),
  fetch('/api/posts').then(r => r.json())
]);
```

#### 2.5 模块系统

```javascript
// export.js
export const PI = 3.14159;
export function add(a, b) { return a + b; }
export default class Calculator { }

// import.js
import Calculator, { PI, add } from './export.js';
import * as utils from './export.js';
```

### 实践建议

1. **练习题目**
   - LeetCode 简单题目（数组、字符串操作）
   - 实现常用数组方法（map、filter、reduce）

2. **项目实践**
   - 使用原生 JS 制作 TODO 列表
   - 使用原生 JS 制作计算器

### 推荐资源

- [MDN JavaScript 教程](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide)
- 《JavaScript 高级程序设计》（红宝书）
- 《你不知道的 JavaScript》系列

---

## 3. 第二阶段：React 入门

> 初步了解 React 的基本概念和开发环境搭建。

### 学习目标

- 理解 React 是什么以及它的核心理念
- 能够搭建 React 开发环境
- 掌握 JSX 语法
- 能够创建简单的 React 组件

### 核心知识点

#### 3.1 React 简介

React 是 Facebook 于 2013 年开源的用于构建用户界面的 JavaScript 库。

**核心理念**：
- 声明式：描述 UI 应该是什么，而不是如何做
- 组件化：将 UI 拆分为独立可复用的组件
- 虚拟 DOM：高效的 DOM 更新机制
- 单向数据流：数据从父组件流向子组件

#### 3.2 开发环境搭建

```bash
# 使用 Create React App（官方推荐）
npx create-react-app my-app
cd my-app
npm start

# 使用 Vite（更快速，推荐）
npm create vite@latest my-app -- --template react
cd my-app
npm install
npm run dev

# 使用 Next.js
npx create-next-app@latest my-app
cd my-app
npm run dev
```

#### 3.3 JSX 语法

```jsx
// JSX 是 JavaScript 的语法扩展
const element = <h1>Hello, World!</h1>;

// 在 JSX 中嵌入表达式
const name = 'Alice';
const element = <h1>Hello, {name}!</h1>;

// 条件渲染
const isLoggedIn = true;
const element = (
  <div>
    {isLoggedIn ? <h1>Welcome back!</h1> : <h1>Please sign in.</h1>}
  </div>
);

// 列表渲染
const items = ['Apple', 'Banana', 'Orange'];
const element = (
  <ul>
    {items.map(item => <li key={item}>{item}</li>)}
  </ul>
);

// 样式
const style = { color: 'red', fontSize: '16px' };
const element = <p style={style}>Styled text</p>;
```

#### 3.4 第一个 React 组件

```jsx
// App.js
function App() {
  return (
    <div className="App">
      <h1>My First React App</h1>
      <p>Welcome to React!</p>
    </div>
  );
}

export default App;

// 带数据的组件
function Welcome({ name, age }) {
  return (
    <div>
      <h1>Hello, {name}!</h1>
      <p>You are {age} years old.</p>
    </div>
  );
}

// 使用组件
function App() {
  return (
    <div>
      <Welcome name="Alice" age={25} />
      <Welcome name="Bob" age={30} />
    </div>
  );
}
```

### 实践建议

1. **环境搭建**
   - 使用 Vite 创建项目
   - 配置开发服务器

2. **基础练习**
   - 创建静态页面（介绍页、作品集）
   - 实现简单的用户卡片组件

3. **理解 JSX**
   - 将 HTML 转换为 JSX
   - 理解 JSX 与 JavaScript 的关系

### 推荐资源

- [React 官方入门教程](https://react.dev/learn)
- Vite 官方文档

---

## 4. 第三阶段：React 核心概念

> 深入理解 React 的核心概念：组件、Props、State、生命周期。

### 学习目标

- 掌握函数组件和类组件的区别
- 理解 Props 的单向数据流
- 掌握 State 的使用和更新机制
- 理解组件生命周期（类组件）

### 核心知识点

#### 4.1 组件类型

```jsx
// 函数组件（推荐）
function Welcome({ name }) {
  return <h1>Hello, {name}!</h1>;
}

// 类组件（仍可用，但不推荐新代码使用）
class Welcome extends React.Component {
  render() {
    return <h1>Hello, {this.props.name}!</h1>;
  }
}
```

#### 4.2 Props 详解

```jsx
// Props 是只读的
function ChildComponent({ name, age, children }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>Age: {age}</p>
      {/* children 是特殊的 prop */}
      <div>{children}</div>
    </div>
  );
}

// 使用
function App() {
  return (
    <ChildComponent name="Alice" age={25}>
      <p>This is children content</p>
    </ChildComponent>
  );
}

// Props 类型检查（可选）
import PropTypes from 'prop-types';

function Button({ text, onClick, disabled }) {
  return <button onClick={onClick} disabled={disabled}>{text}</button>;
}

Button.propTypes = {
  text: PropTypes.string.isRequired,
  onClick: PropTypes.func,
  disabled: PropTypes.bool
};
```

#### 4.3 State 详解

```jsx
// 使用 useState（函数组件）
function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}

// 多个 state
function Form() {
  const [name, setName] = useState('');
  const [email, setEmail] = useState('');
  const [age, setAge] = useState(0);

  return (
    <form>
      <input value={name} onChange={e => setName(e.target.value)} />
      <input value={email} onChange={e => setEmail(e.target.value)} />
      <input value={age} onChange={e => setAge(e.target.value)} />
    </form>
  );
}

// 对象 state
function Form() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    age: 0
  });

  const handleChange = (field, value) => {
    setFormData(prev => ({
      ...prev,
      [field]: value
    }));
  };

  return (
    <form>
      <input
        value={formData.name}
        onChange={e => handleChange('name', e.target.value)}
      />
    </form>
  );
}
```

#### 4.4 组件通信

```jsx
// 父 -> 子：通过 props
function Parent() {
  const data = 'Hello from Parent';
  return <Child data={data} />;
}

function Child({ data }) {
  return <p>{data}</p>;
}

// 子 -> 父：通过回调函数
function Parent() {
  const [message, setMessage] = useState('');

  return (
    <div>
      <p>Message: {message}</p>
      <Child onMessage={setMessage} />
    </div>
  );
}

function Child({ onMessage }) {
  const handleClick = () => onMessage('Hello from Child');
  return <button onClick={handleClick}>Send</button>;
}
```

### 实践建议

1. **基础练习**
   - 实现一个计数器
   - 实现一个 TODO 列表
   - 实现一个简单的表单

2. **进阶练习**
   - 多个组件之间的数据传递
   - 状态提升练习

---

## 5. 第四阶段：Hooks 深入

> 深入学习 React Hooks 系统，掌握各种 Hook 的使用场景和原理。

### 学习目标

- 掌握常用 Hooks（useState、useEffect、useRef）
- 理解 Hooks 的规则
- 能够创建自定义 Hooks
- 掌握 useMemo 和 useCallback 的使用场景

### 核心知识点

#### 5.1 useState 深入

```jsx
// 函数式更新（重要）
function Counter() {
  const [count, setCount] = useState(0);

  // 正确：使用函数更新
  const increment = () => setCount(prev => prev + 1);

  // 错误：依赖外部变量
  // const increment = () => setCount(count + 1);

  return <button onClick={increment}>{count}</button>;
}

// 惰性初始化
function ExpensiveComponent() {
  // 每次渲染都会执行 expensiveComputation()
  // const [data, setData] = useState(expensiveComputation());

  // 只在首次渲染时执行
  const [data, setData] = useState(() => expensiveComputation());

  return <div>{data}</div>;
}

function expensiveComputation() {
  // 耗时计算
  return 'computed result';
}
```

#### 5.2 useEffect 详解

```jsx
// 基础用法
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    // 组件挂载后执行
    fetch(`/api/users/${userId}`)
      .then(res => res.json())
      .then(setUser);
  }, [userId]); // 依赖数组

  return <div>{user?.name}</div>;
}

// 清理函数
function MouseTracker() {
  const [position, setPosition] = useState({ x: 0, y: 0 });

  useEffect(() => {
    const handleMove = (e) => {
      setPosition({ x: e.clientX, y: e.clientY });
    };

    window.addEventListener('mousemove', handleMove);

    // 清理函数：组件卸载时执行
    return () => {
      window.removeEventListener('mousemove', handleMove);
    };
  }, []);

  return <div>{position.x}, {position.y}</div>;
}

// 常见场景
function DataFetcher({ url }) {
  const [data, setData] = useState(null);

  // 数据获取
  useEffect(() => {
    const controller = new AbortController();

    fetch(url, { signal: controller.signal })
      .then(r => r.json())
      .then(setData)
      .catch(err => {
        if (err.name !== 'AbortError') {
          console.error(err);
        }
      });

    return () => controller.abort();
  }, [url]);

  return <div>{JSON.stringify(data)}</div>;
}
```

#### 5.3 useRef 详解

```jsx
// 访问 DOM
function FocusInput() {
  const inputRef = useRef(null);

  const handleFocus = () => {
    inputRef.current.focus();
  };

  return (
    <div>
      <input ref={inputRef} type="text" />
      <button onClick={handleFocus}>Focus Input</button>
    </div>
  );
}

// 存储不需要触发渲染的值
function Timer() {
  const [count, setCount] = useState(0);
  const countRef = useRef(0);
  const timerRef = useRef(null);

  const start = () => {
    timerRef.current = setInterval(() => {
      countRef.current += 1;
      console.log('Ref count:', countRef.current);
    }, 1000);
  };

  const stop = () => {
    clearInterval(timerRef.current);
  };

  return (
    <div>
      <p>Ref count: {countRef.current}</p>
      <button onClick={start}>Start</button>
      <button onClick={stop}>Stop</button>
    </div>
  );
}

// 保存上一次的 props/state
function UsePrevious({ value }) {
  const prevRef = useRef();

  useEffect(() => {
    prevRef.current = value;
  }, [value]);

  return <div>Previous: {prevRef.current}, Current: {value}</div>;
}
```

#### 5.4 useMemo 和 useCallback

```jsx
// useMemo：缓存计算结果
function ExpensiveList({ items, filter }) {
  // 只有 items 或 filter 变化时才重新计算
  const filteredItems = useMemo(() => {
    return items.filter(item =>
      item.name.toLowerCase().includes(filter.toLowerCase())
    );
  }, [items, filter]);

  return (
    <ul>
      {filteredItems.map(item => (
        <li key={item.id}>{item.name}</li>
      ))}
    </ul>
  );
}

// useCallback：缓存函数
function Parent() {
  const [count, setCount] = useState(0);

  // 缓存回调函数，避免子组件不必要渲染
  const handleClick = useCallback(() => {
    console.log('clicked');
  }, []);

  return <Child onClick={handleClick} />;
}

const Child = React.memo(({ onClick }) => {
  console.log('Child rendered');
  return <button onClick={onClick}>Click</button>;
});
```

#### 5.5 自定义 Hooks

```jsx
// useLocalStorage
function useLocalStorage(key, initialValue) {
  const [value, setValue] = useState(() => {
    const stored = localStorage.getItem(key);
    return stored ? JSON.parse(stored) : initialValue;
  });

  useEffect(() => {
    localStorage.setItem(key, JSON.stringify(value));
  }, [key, value]);

  return [value, setValue];
}

// useDebounce
function useDebounce(value, delay) {
  const [debounced, setDebounced] = useState(value);

  useEffect(() => {
    const timer = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);

  return debounced;
}

// useFetch
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    fetch(url)
      .then(r => r.json())
      .then(setData)
      .catch(setError)
      .finally(() => setLoading(false));
  }, [url]);

  return { data, loading, error };
}
```

### 实践建议

1. **Hooks 练习**
   - 实现 useDebounce
   - 实现 useScrollPosition
   - 实现 useWindowSize
   - 实现 useClickOutside

2. **理解原理**
   - 理解 Hooks 为什么必须在顶层调用
   - 理解闭包问题及其解决方案

---

## 6. 第五阶段：状态管理

> 学习 React 状态管理方案，从简单的 useState 到复杂的状态管理库。

### 学习目标

- 掌握 useState + useContext 方案
- 理解 Context 的使用场景和限制
- 掌握 Redux 或 Zustand 的使用
- 能够根据场景选择合适的状态管理方案

### 核心知识点

#### 6.1 useState + useContext

```jsx
// 创建 Context
const ThemeContext = React.createContext();
const AuthContext = React.createContext();

// Provider
function AppProvider({ children }) {
  const [theme, setTheme] = useState('light');
  const [user, setUser] = useState(null);

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      <AuthContext.Provider value={{ user, setUser }}>
        {children}
      </AuthContext.Provider>
    </ThemeContext.Provider>
  );
}

// 使用 Context
function ThemedButton() {
  const { theme, setTheme } = useContext(ThemeContext);

  return (
    <button
      onClick={() => setTheme(t => t === 'light' ? 'dark' : 'light')}
      className={theme}
    >
      Toggle Theme
    </button>
  );
}
```

#### 6.2 Redux Toolkit

```bash
# 安装
npm install @reduxjs/toolkit react-redux
```

```javascript
// store/index.js
import { configureStore } from '@reduxjs/toolkit';
import counterReducer from './counterSlice';
import userReducer from './userSlice';

export const store = configureStore({
  reducer: {
    counter: counterReducer,
    user: userReducer
  }
});

// counterSlice.js
import { createSlice } from '@reduxjs/toolkit';

const counterSlice = createSlice({
  name: 'counter',
  initialState: { value: 0 },
  reducers: {
    increment: (state) => { state.value += 1; },
    decrement: (state) => { state.value -= 1; },
    incrementByAmount: (state, action) => {
      state.value += action.payload;
    }
  }
});

export const { increment, decrement, incrementByAmount } = counterSlice.actions;
export default counterSlice.reducer;

// 组件中使用
import { useSelector, useDispatch } from 'react-redux';
import { increment, decrement } from './counterSlice';

function Counter() {
  const count = useSelector(state => state.counter.value);
  const dispatch = useDispatch();

  return (
    <div>
      <button onClick={() => dispatch(decrement())}>-</button>
      <span>{count}</span>
      <button onClick={() => dispatch(increment())}>+</button>
    </div>
  );
}
```

#### 6.3 Zustand（轻量级方案）

```bash
npm install zustand
```

```javascript
// store/useStore.js
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  user: null,
  increment: () => set(state => ({ count: state.count + 1 })),
  decrement: () => set(state => ({ count: state.count - 1 })),
  setUser: (user) => set({ user }),
  fetchUser: async (id) => {
    const res = await fetch(`/api/users/${id}`);
    const user = await res.json();
    set({ user });
  }
}));

// 组件中使用
function Counter() {
  const { count, increment, decrement } = useStore();

  return (
    <div>
      <button onClick={decrement}>-</button>
      <span>{count}</span>
      <button onClick={increment}>+</button>
    </div>
  );
}

// 派生状态
function DoubleCount() {
  const double = useStore(state => state.count * 2);
  return <div>Double: {double}</div>;
}
```

#### 6.4 状态管理方案选择

| 场景 | 推荐方案 |
|------|----------|
| 小型应用 | useState + useContext |
| 中型应用 | Zustand |
| 大型应用 | Redux Toolkit |
| 需要响应式 | MobX |
| 原子化状态 | Jotai |

### 实践建议

1. **小型项目**
   - 使用 useState 管理组件状态
   - 使用 Context 管理全局配置（主题、语言）

2. **中型项目**
   - 引入 Zustand
   - 分离状态和视图逻辑

3. **大型项目**
   - 使用 Redux Toolkit
   - 合理拆分 slice
   - 使用 Redux DevTools 调试

---

## 7. 第六阶段：路由与数据请求

> 掌握 React 路由和数据请求的实现方案。

### 学习目标

- 掌握 React Router 的使用
- 理解路由守卫和嵌套路由
- 掌握数据请求方案（fetch/axios）
- 理解数据请求的状态管理

### 核心知识点

#### 7.1 React Router v6

```bash
npm install react-router-dom
```

```jsx
import { BrowserRouter, Routes, Route, Link, Outlet } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Layout />}>
          <Route index element={<Home />} />
          <Route path="about" element={<About />} />
          <Route path="users/*" element={<Users />} />
          <Route path="*" element={<NotFound />} />
        </Route>
      </Routes>
    </BrowserRouter>
  );
}

function Layout() {
  return (
    <div>
      <nav>
        <Link to="/">Home</Link>
        <Link to="/about">About</Link>
      </nav>
      <Outlet />
    </div>
  );
}

// 动态路由
function UserDetail() {
  const { id } = useParams();
  return <div>User ID: {id}</div>;
}

// 路由守卫
function PrivateRoute({ children, isAuthenticated }) {
  return isAuthenticated ? children : <Navigate to="/login" />;
}
```

#### 7.2 数据请求

```jsx
// 使用 fetch
function UserList() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetch('/api/users')
      .then(r => r.json())
      .then(data => {
        setUsers(data);
        setLoading(false);
      })
      .catch(err => {
        console.error(err);
        setLoading(false);
      });
  }, []);

  if (loading) return <div>Loading...</div>;

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}

// 使用 axios
import axios from 'axios';

function UserList() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    axios.get('/api/users')
      .then(res => setUsers(res.data));
  }, []);

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}

// 推荐：React Query
import { useQuery } from '@tanstack/react-query';

function UserList() {
  const { data, isLoading, error } = useQuery({
    queryKey: ['users'],
    queryFn: () => fetch('/api/users').then(r => r.json())
  });

  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;

  return (
    <ul>
      {data.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}
```

### 实践建议

1. **路由练习**
   - 实现博客系统的路由
   - 实现后台管理的路由

2. **数据请求**
   - 实现 CRUD 操作
   - 使用 React Query 优化数据请求

---

## 8. 第七阶段：性能优化

> 学习 React 性能优化的技巧和方法。

### 学习目标

- 理解 React 渲染机制
- 掌握性能优化技巧
- 能够使用 React DevTools Profiler 分析性能
- 理解代码分割和懒加载

### 核心知识点

#### 8.1 React.memo

```jsx
// 缓存组件，避免不必要的重新渲染
const Button = React.memo(({ onClick, children }) => {
  console.log('Button rendered');
  return <button onClick={onClick}>{children}</button>;
});

// 自定义比较函数
const Button = React.memo(
  ({ onClick, children }) => {
    return <button onClick={onClick}>{children}</button>;
  },
  (prevProps, nextProps) => {
    // 返回 true 表示相等，不重新渲染
    return prevProps.onClick === nextProps.onClick;
  }
);
```

#### 8.2 代码分割

```jsx
import { lazy, Suspense } from 'react';

// 懒加载组件
const HeavyComponent = lazy(() => import('./HeavyComponent'));

function App() {
  return (
    <Suspense fallback={<Loading />}>
      <HeavyComponent />
    </Suspense>
  );
}

function Loading() {
  return <div>Loading...</div>;
}

// 路由级别的代码分割
const Home = lazy(() => import('./pages/Home'));
const About = lazy(() => import('./pages/About'));

function App() {
  return (
    <Routes>
      <Route path="/" element={<Suspense fallback={<Loading />}><Home /></Suspense>} />
    </Routes>
  );
}
```

#### 8.3 React DevTools Profiler

```javascript
// 使用 React DevTools Profiler 分析性能
// 1. 安装 React DevTools 浏览器扩展
// 2. 打开 DevTools -> Profiler 标签
// 3. 点击 "Start Recording"
// 4. 进行操作
// 5. 点击 "Stop Recording"
// 6. 分析结果
```

#### 8.4 性能优化清单

1. **减少不必要的渲染**
   - 使用 React.memo 包装纯组件
   - 正确设置 useMemo/useCallback 依赖

2. **优化计算**
   - 使用 useMemo 缓存计算结果
   - 避免在渲染中执行复杂计算

3. **代码分割**
   - 使用 React.lazy 懒加载
   - 使用 React.Suspense

4. **虚拟列表**
   - 使用 react-window 处理长列表

5. **图片优化**
   - 使用 WebP 格式
   - 实现懒加载

---

## 9. 第八阶段：TypeScript 与 React

> 学习 TypeScript 与 React 的结合使用。

### 学习目标

- 掌握 React 组件的 TypeScript 类型定义
- 理解泛型组件的编写
- 能够为自定义 Hooks 添加类型

### 核心知识点

#### 9.1 组件 Props 类型

```tsx
// 基本类型
interface ButtonProps {
  text: string;
  onClick: () => void;
  disabled?: boolean;
  variant?: 'primary' | 'secondary';
}

function Button({ text, onClick, disabled = false, variant = 'primary' }: ButtonProps) {
  return (
    <button
      className={`btn btn-${variant}`}
      onClick={onClick}
      disabled={disabled}
    >
      {text}
    </button>
  );
}

// 使用
<Button text="Click me" onClick={() => console.log('clicked')} />
```

#### 9.2 State 类型

```tsx
// useState 类型推断
const [count, setCount] = useState(0); // number

// 显式类型
const [user, setUser] = useState<User | null>(null);
const [loading, setLoading] = useState<boolean>(true);

// 复杂状态
interface Todo {
  id: number;
  text: string;
  completed: boolean;
}

const [todos, setTodos] = useState<Todo[]>([]);
const [filter, setFilter] = useState<'all' | 'active' | 'completed'>('all');
```

#### 9.3 事件处理类型

```tsx
// 表单事件
function Form() {
  const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
  };

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    console.log(e.target.value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input onChange={handleChange} />
    </form>
  );
}

// 点击事件
function Button() {
  const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {
    console.log(e.currentTarget);
  };

  return <button onClick={handleClick}>Click</button>;
}
```

#### 9.4 自定义 Hook 类型

```tsx
function useLocalStorage<T>(key: string, initialValue: T) {
  const [value, setValue] = useState<T>(() => {
    const stored = localStorage.getItem(key);
    return stored ? JSON.parse(stored) : initialValue;
  });

  useEffect(() => {
    localStorage.setItem(key, JSON.stringify(value));
  }, [key, value]);

  return [value, setValue] as const;
}

// 使用
const [name, setName] = useLocalStorage<string>('name', '');
```

### 实践建议

1. **类型练习**
   - 将现有 JavaScript React 项目迁移到 TypeScript
   - 为常用组件添加类型定义

2. **最佳实践**
   - 避免使用 any
   - 使用 strict 模式
   - 充分利用类型推断

---

## 9. 第九阶段：服务端渲染与 Next.js

> 学习服务端渲染（SSR）概念和 Next.js 框架。

### 学习目标

- 理解 SSR vs CSR 的区别
- 掌握 Next.js 基础
- 理解 App Router 的使用
- 掌握静态生成和 ISR

### 核心知识点

#### 9.1 SSR vs CSR

| 特性 | CSR | SSR |
|------|-----|-----|
| 首屏加载 | 慢 | 快 |
| SEO | 差 | 好 |
| 交互性 | 需要 hydration | 需要 hydration |
| 服务器负载 | 低 | 高 |

#### 9.2 Next.js 基础

```bash
# 创建项目
npx create-next-app@latest my-app
# 或
npm create vite@latest my-app -- --template next-ts
```

```tsx
// pages/index.tsx (Pages Router)
import { GetServerSideProps } from 'next';

interface Props {
  data: { title: string };
}

export const getServerSideProps: GetServerSideProps<Props> = async () => {
  const res = await fetch('https://api.example.com/data');
  const data = await res.json();

  return {
    props: { data }
  };
};

export default function Home({ data }: Props) {
  return <h1>{data.title}</h1>;
}
```

#### 9.3 Next.js App Router (推荐)

```tsx
// app/page.tsx
async function getData() {
  const res = await fetch('https://api.example.com/data', {
    cache: 'no-store' // 禁用缓存
  });
  return res.json();
}

export default async function Page() {
  const data = await getData();

  return <h1>{data.title}</h1>;
}

// app/layout.tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh">
      <body>{children}</body>
    </html>
  );
}

// app/page.tsx (Client Component)
'use client';

import { useState } from 'react';

export default function Counter() {
  const [count, setCount] = useState(0);

  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}
```

#### 9.4 静态生成与 ISR

```tsx
// 静态生成 (SSG)
export async function generateStaticParams() {
  const posts = await fetch('https://api.example.com/posts').then(r => r.json());

  return posts.map((post: any) => ({
    slug: post.slug
  }));
}

export default async function Post({ params }: { params: { slug: string } }) {
  const post = await fetch(`https://api.example.com/posts/${params.slug}`).then(r => r.json());

  return <article><h1>{post.title}</h1></article>;
}

// ISR (增量静态再生成)
export const revalidate = 60; // 60 秒重新生成一次

export default async function Page() {
  const data = await fetch('https://api.example.com/data', {
    next: { revalidate: 60 }
  }).then(r => r.json());

  return <div>{data.title}</div>;
}
```

### 实践建议

1. **项目练习**
   - 使用 Next.js 重写现有 React 项目
   - 实现博客系统

2. **部署**
   - 部署到 Vercel
   - 配置 CI/CD

---

## 10. 第十阶段：测试与工程化

> 学习 React 项目的测试和工程化最佳实践。

### 学习目标

- 掌握 React 组件测试方法
- 理解单元测试和集成测试
- 掌握 E2E 测试
- 理解 CI/CD 和代码质量工具

### 核心知识点

#### 10.1 单元测试

```bash
# 安装测试库
npm install --save-dev @testing-library/react @testing-library/jest-dom jest-environment-jsdom
```

```tsx
// Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import Button from './Button';

describe('Button', () => {
  test('renders button with text', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  test('calls onClick when clicked', async () => {
    const handleClick = jest.fn();
    const user = userEvent.setup();

    render(<Button onClick={handleClick}>Click me</Button>);

    await user.click(screen.getByText('Click me'));

    expect(handleClick).toHaveBeenCalledTimes(1);
  });

  test('is disabled when disabled prop is true', () => {
    render(<Button disabled>Click me</Button>);
    expect(screen.getByRole('button')).toBeDisabled();
  });
});
```

#### 10.2 Hooks 测试

```tsx
// useCounter.test.ts
import { renderHook, act } from '@testing-library/react';
import { useCounter } from './useCounter';

test('increments count', () => {
  const { result } = renderHook(() => useCounter());

  act(() => {
    result.current.increment();
  });

  expect(result.current.count).toBe(1);
});

test('initializes with custom value', () => {
  const { result } = renderHook(() => useCounter(10));

  expect(result.current.count).toBe(10);
});
```

#### 10.3 E2E 测试

```bash
# 安装 Cypress
npm install --save-dev cypress
npx cypress open
```

```javascript
// cypress/e2e/app.cy.js
describe('My App', () => {
  it('should display welcome message', () => {
    cy.visit('http://localhost:3000');
    cy.contains('Welcome').should('be.visible');
  });

  it('should increment counter', () => {
    cy.visit('http://localhost:3000');
    cy.get('button').click();
    cy.contains('1').should('be.visible');
  });
});
```

#### 10.4 代码质量工具

```bash
# ESLint
npm install --save-dev eslint eslint-config-react-app

# Prettier
npm install --save-dev prettier

# husky + lint-staged
npm install --save-dev husky lint-staged
```

```json
// .eslintrc.json
{
  "extends": ["react-app"],
  "rules": {
    "no-unused-vars": "warn",
    "react-hooks/exhaustive-deps": "warn"
  }
}

// .prettierrc
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```

### 实践建议

1. **测试覆盖**
   - 为关键业务组件编写测试
   - 使用 TDD 开发模式

2. **CI/CD**
   - 配置 GitHub Actions
   - 自动运行测试和 lint

---

## 11. 实战项目建议

### 项目难度分级

#### 初级项目（1-2周）

1. **TODO 列表**
   - 添加、删除、编辑任务
   - 标记完成状态
   - 本地存储持久化

2. **天气预报应用**
   - 获取用户位置
   - 调用天气 API
   - 展示天气信息

3. **计算器**
   - 基本数学运算
   - 历史记录

#### 中级项目（2-4周）

1. **博客系统**
   - 文章列表和详情页
   - Markdown 支持
   - 评论功能
   - Next.js + 静态生成

2. **电商商品列表**
   - 商品展示和筛选
   - 购物车功能
   - 状态管理（Zustand）

3. **即时通讯应用**
   - 实时消息
   - 用户列表
   - WebSocket 集成

#### 高级项目（4-8周）

1. **后台管理系统**
   - 用户管理
   - 数据可视化
   - RBAC 权限控制

2. **社交媒体应用**
   - 用户认证
   - 发帖、点赞、评论
   - 实时通知

3. **协作工具**
   - 实时同步
   - 多人协作编辑
   - WebSocket + CRDT

### 项目开发流程

1. **需求分析**
   - 明确功能需求
   - 绘制原型图

2. **技术选型**
   - 选择合适的技术栈
   - 决定状态管理方案

3. **项目搭建**
   - 初始化项目
   - 配置开发环境

4. **核心功能开发**
   - 实现主要功能
   - 遵循代码规范

5. **测试与优化**
   - 编写测试用例
   - 性能优化

6. **部署上线**
   - 选择部署平台
   - 配置 CI/CD

---

## 12. 学习资源汇总

### 官方文档

- [React 官方文档](https://react.dev)
- [React Router 文档](https://reactrouter.com)
- [Redux 文档](https://redux.js.org)
- [Next.js 文档](https://nextjs.org/docs)
- [TypeScript 文档](https://www.typescriptlang.org/docs/)

### 在线教程

- [React 入门教程](https://react.dev/learn)
- [React Hooks 教程](https://react.dev/reference/react)
- [Epic React](https://epicreact.dev/)

### 视频教程

- [React 官方 YouTube](https://www.youtube.com/@React)
- [Fireship React 教程](https://www.youtube.com/watch?vKe90Tje7VS0)
- [JavaScript Mastery](https://www.youtube.com/@JavaScriptMastery)

### 工具库推荐

| 类别 | 推荐 |
|------|------|
| 状态管理 | Redux Toolkit, Zustand, Jotai |
| 数据请求 | React Query, SWR, Axios |
| UI 组件 | Material UI, Ant Design, Chakra UI |
| 动画 | Framer Motion, React Spring |
| 表单 | React Hook Form, Formik |
| 测试 | Jest, React Testing Library, Cypress |
| 样式 | Tailwind CSS, Styled Components, CSS Modules |

### 社区与博客

- [React 官方博客](https://react.dev/blog)
- [Kent C. Dodds 博客](https://kentcdodds.com/blog)
- [Overreacted (Dan Abramov)](https://overreacted.io/)
- [Josh Comeau 博客](https://www.joshwcomeau.com/)

### 面试准备

- [前端面试题汇总 - React 部分](面试题汇总.md)
- [LeetCode 刷题](https://leetcode.com)
- [Front End Interview Handbook](https://frontendinterviewhandbook.com/)

---

## 总结

### 学习建议

1. **循序渐进**
   - 不要急于求成，扎实掌握每个阶段的知识

2. **动手实践**
   - 光看不动手是不够的，必须亲自coding

3. **项目驱动**
   - 通过项目来学习，遇到问题再深入研究

4. **保持更新**
   - React 生态变化快，关注官方动态

5. **社区交流**
   - 参与开源项目，加入技术社区

### 下一步方向

- **React Native**: 移动端开发
- **Electron**: 桌面应用开发
- **React Server Components**: 服务端组件
- **React 编译器**: 自动优化

---

> 祝你在 React 学习之路上一帆风顺！如果有任何问题，欢迎随时交流探讨。
