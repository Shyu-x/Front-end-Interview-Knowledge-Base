# 第8章：实战项目开发

> 本章将通过一个完整的实战项目，将前面学到的知识串联起来

---

## 8.1 项目概述

### 项目：待办事项管理系统

我们将创建一个功能完整的待办事项管理应用，包含以下功能：

- [ ] 待办事项的增删改查
- [ ] 标记完成/未完成
- [ ] 分类管理
- [ ] 数据持久化（localStorage）
- [ ] 响应式设计

### 技术栈

| 技术 | 用途 |
|------|------|
| React 19 | UI 框架 |
| Zustand | 状态管理 |
| Ant Design | UI 组件库 |
| Vite | 构建工具 |
| localStorage | 数据持久化 |

---

## 8.2 项目初始化

### 创建项目

```bash
# 创建项目
npm create vite@latest todo-app -- --template react

# 进入项目目录
cd todo-app

# 安装依赖
npm install zustand antd @ant-design/icons dayjs

# 启动开发服务器
npm run dev
```

### 项目结构

```
src/
├── components/           # 组件目录
│   ├── TodoList.jsx     # 待办列表组件
│   ├── TodoItem.jsx     # 待办项组件
│   ├── TodoForm.jsx     # 添加/编辑表单
│   ├── CategoryFilter.jsx # 分类筛选
│   └── Header.jsx       # 页头组件
├── stores/              # 状态管理
│   └── todoStore.js     # Zustand Store
├── utils/              # 工具函数
│   └── storage.js       # localStorage 工具
├── App.jsx             # 主组件
├── App.css             # 样式
└── main.jsx            # 入口文件
```

---

## 8.3 状态管理设计

### 创建 Todo Store

```javascript
// stores/todoStore.js
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

const useTodoStore = create(
  persist(
    (set, get) => ({
      // 状态
      todos: [],
      filter: 'all', // all, active, completed
      category: 'all',

      // 添加待办
      addTodo: (text, category = '默认') => {
        const newTodo = {
          id: Date.now().toString(),
          text,
          category,
          completed: false,
          createdAt: new Date().toISOString()
        }
        set((state) => ({
          todos: [newTodo, ...state.todos]
        }))
      },

      // 删除待办
      deleteTodo: (id) => {
        set((state) => ({
          todos: state.todos.filter((todo) => todo.id !== id)
        }))
      },

      // 切换完成状态
      toggleTodo: (id) => {
        set((state) => ({
          todos: state.todos.map((todo) =>
            todo.id === id ? { ...todo, completed: !todo.completed } : todo
          )
        }))
      },

      // 编辑待办
      editTodo: (id, text) => {
        set((state) => ({
          todos: state.todos.map((todo) =>
            todo.id === id ? { ...todo, text } : todo
          )
        }))
      },

      // 清除已完成
      clearCompleted: () => {
        set((state) => ({
          todos: state.todos.filter((todo) => !todo.completed)
        }))
      },

      // 设置筛选
      setFilter: (filter) => {
        set({ filter })
      },

      // 设置分类
      setCategory: (category) => {
        set({ category })
      },

      // 获取筛选后的待办
      getFilteredTodos: () => {
        const { todos, filter, category } = get()

        return todos.filter((todo) => {
          // 分类筛选
          if (category !== 'all' && todo.category !== category) {
            return false
          }

          // 状态筛选
          if (filter === 'active' && todo.completed) {
            return false
          }
          if (filter === 'completed' && !todo.completed) {
            return false
          }

          return true
        })
      },

      // 获取统计
      getStats: () => {
        const { todos } = get()
        const total = todos.length
        const completed = todos.filter((t) => t.completed).length
        const active = total - completed

        return { total, completed, active }
      }
    }),
    {
      name: 'todo-storage' // localStorage 键名
    }
  )
)

export default useTodoStore
```

---

## 8.4 组件开发

### 1. Header 组件

```jsx
// components/Header.jsx
import { Typography, Space } from 'antd'

const { Title } = Typography

function Header({ stats }) {
  return (
    <div className="header">
      <Title level={2}>📝 待办事项</Title>
      <Space>
        <span>总计: {stats.total}</span>
        <span>已完成: {stats.completed}</span>
        <span>进行中: {stats.active}</span>
      </Space>
    </div>
  )
}

export default Header
```

### 2. TodoForm 组件

```jsx
// components/TodoForm.jsx
import { Form, Input, Select, Button } from 'antd'
import { PlusOutlined } from '@ant-design/icons'

const { Option } = Select

function TodoForm({ onAdd, categories }) {
  const [form] = Form.useForm()

  const onFinish = (values) => {
    onAdd(values.text, values.category)
    form.resetFields()
  }

  return (
    <Form
      form={form}
      layout="inline"
      onFinish={onFinish}
      initialValues={{ category: '默认' }}
    >
      <Form.Item
        name="text"
        rules={[{ required: true, message: '请输入待办事项' }]}
        style={{ flex: 1 }}
      >
        <Input placeholder="添加新的待办事项..." />
      </Form.Item>

      <Form.Item name="category" style={{ width: 120 }}>
        <Select>
          {categories.map((cat) => (
            <Option key={cat} value={cat}>
              {cat}
            </Option>
          ))}
        </Select>
      </Form.Item>

      <Form.Item>
        <Button type="primary" htmlType="submit" icon={<PlusOutlined />}>
          添加
        </Button>
      </Form.Item>
    </Form>
  )
}

export default TodoForm
```

### 3. TodoItem 组件

```jsx
// components/TodoItem.jsx
import { Checkbox, Input, Button, Popconfirm, Tag, Space } from 'antd'
import { DeleteOutlined, EditOutlined, CheckOutlined, CloseOutlined } from '@ant-design/icons'
import { useState } from 'react'

function TodoItem({ todo, onToggle, onDelete, onEdit }) {
  const [isEditing, setIsEditing] = useState(false)
  const [editText, setEditText] = useState(todo.text)

  const handleEdit = () => {
    if (editText.trim()) {
      onEdit(todo.id, editText)
      setIsEditing(false)
    }
  }

  const cancelEdit = () => {
    setEditText(todo.text)
    setIsEditing(false)
  }

  return (
    <div className={`todo-item ${todo.completed ? 'completed' : ''}`}>
      <Checkbox
        checked={todo.completed}
        onChange={() => onToggle(todo.id)}
      >
        {isEditing ? (
          <Space>
            <Input
              value={editText}
              onChange={(e) => setEditText(e.target.value)}
              onPressEnter={handleEdit}
              size="small"
              style={{ width: 200 }}
            />
            <Button
              type="primary"
              size="small"
              icon={<CheckOutlined />}
              onClick={handleEdit}
            />
            <Button
              size="small"
              icon={<CloseOutlined />}
              onClick={cancelEdit}
            />
          </Space>
        ) : (
          <span className="todo-text">{todo.text}</span>
        )}
      </Checkbox>

      {!isEditing && (
        <Space>
          <Tag color="blue">{todo.category}</Tag>

          <Button
            type="text"
            size="small"
            icon={<EditOutlined />}
            onClick={() => setIsEditing(true)}
          />

          <Popconfirm
            title="确认删除?"
            onConfirm={() => onDelete(todo.id)}
          >
            <Button
              type="text"
              size="small"
              danger
              icon={<DeleteOutlined />}
            />
          </Popconfirm>
        </Space>
      )}
    </div>
  )
}

export default TodoItem
```

### 4. TodoList 组件

```jsx
// components/TodoList.jsx
import { List, Empty } from 'antd'
import TodoItem from './TodoItem'

function TodoList({ todos, onToggle, onDelete, onEdit }) {
  if (todos.length === 0) {
    return <Empty description="暂无待办事项" />
  }

  return (
    <List
      dataSource={todos}
      renderItem={(todo) => (
        <TodoItem
          todo={todo}
          onToggle={onToggle}
          onDelete={onDelete}
          onEdit={onEdit}
        />
      )}
    />
  )
}

export default TodoList
```

### 5. CategoryFilter 组件

```jsx
// components/CategoryFilter.jsx
import { Radio, Space, Button } from 'antd'

function CategoryFilter({ filter, category, categories, onFilterChange, onCategoryChange, onClearCompleted }) {
  return (
    <div className="filter-bar">
      <Space>
        <Radio.Group
          value={category}
          onChange={(e) => onCategoryChange(e.target.value)}
          buttonStyle="solid"
        >
          <Radio.Button value="all">全部</Radio.Button>
          {categories.map((cat) => (
            <Radio.Button key={cat} value={cat}>
              {cat}
            </Radio.Button>
          ))}
        </Radio.Group>
      </Space>

      <Space>
        <Radio.Group
          value={filter}
          onChange={(e) => onFilterChange(e.target.value)}
        >
          <Radio.Button value="all">全部</Radio.Button>
          <Radio.Button value="active">进行中</Radio.Button>
          <Radio.Button value="completed">已完成</Radio.Button>
        </Radio.Group>

        <Button danger onClick={onClearCompleted}>
          清除已完成
        </Button>
      </Space>
    </div>
  )
}

export default CategoryFilter
```

---

## 8.5 主组件

### App.jsx

```jsx
// App.jsx
import { useState, useMemo } from 'react'
import { Card, message } from 'antd'
import Header from './components/Header'
import TodoForm from './components/TodoForm'
import TodoList from './components/TodoList'
import CategoryFilter from './components/CategoryFilter'
import useTodoStore from './stores/todoStore'
import './App.css'

function App() {
  // 从 Store 获取状态和方法
  const {
    todos,
    filter,
    category,
    addTodo,
    deleteTodo,
    toggleTodo,
    editTodo,
    clearCompleted,
    setFilter,
    setCategory,
    getFilteredTodos,
    getStats
  } = useTodoStore()

  // 获取筛选后的待办
  const filteredTodos = getFilteredTodos()

  // 获取统计
  const stats = getStats()

  // 获取所有分类
  const categories = useMemo(() => {
    const cats = new Set(todos.map((t) => t.category))
    return ['默认', ...Array.from(cats).filter((c) => c !== '默认')]
  }, [todos])

  // 处理添加
  const handleAdd = (text, cat) => {
    addTodo(text, cat)
    message.success('添加成功')
  }

  // 处理删除
  const handleDelete = (id) => {
    deleteTodo(id)
    message.success('删除成功')
  }

  // 处理切换完成状态
  const handleToggle = (id) => {
    toggleTodo(id)
  }

  // 处理编辑
  const handleEdit = (id, text) => {
    editTodo(id, text)
    message.success('更新成功')
  }

  // 处理清除已完成
  const handleClearCompleted = () => {
    clearCompleted()
    message.success('已清除已完成项')
  }

  return (
    <div className="app">
      <Card className="todo-card">
        <Header stats={stats} />

        <TodoForm onAdd={handleAdd} categories={categories} />

        <CategoryFilter
          filter={filter}
          category={category}
          categories={categories}
          onFilterChange={setFilter}
          onCategoryChange={setCategory}
          onClearCompleted={handleClearCompleted}
        />

        <TodoList
          todos={filteredTodos}
          onToggle={handleToggle}
          onDelete={handleDelete}
          onEdit={handleEdit}
        />
      </Card>
    </div>
  )
}

export default App
```

---

## 8.6 样式文件

### App.css

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial,
    'Noto Sans', sans-serif;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  min-height: 100vh;
  padding: 40px 20px;
}

.app {
  max-width: 800px;
  margin: 0 auto;
}

.todo-card {
  border-radius: 16px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
  padding-bottom: 16px;
  border-bottom: 1px solid #f0f0f0;
}

.header h2 {
  margin: 0 !important;
}

.filter-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: 16px 0;
  padding: 12px 0;
  border-top: 1px solid #f0f0f0;
  border-bottom: 1px solid #f0f0f0;
}

.todo-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 0;
  border-bottom: 1px solid #f5f5f5;
  transition: all 0.3s;
}

.todo-item:hover {
  background: #fafafa;
}

.todo-item.completed .todo-text {
  text-decoration: line-through;
  color: #999;
}

.todo-item .ant-checkbox-wrapper {
  flex: 1;
}

.todo-text {
  margin-left: 8px;
  font-size: 15px;
}

/* 响应式 */
@media (max-width: 768px) {
  .header {
    flex-direction: column;
    gap: 12px;
  }

  .filter-bar {
    flex-direction: column;
    gap: 12px;
  }

  .ant-form-item {
    margin-bottom: 12px;
  }
}
```

---

## 8.7 项目运行效果

项目完成后，你将看到一个功能完整的待办事项管理系统：

1. **添加待办**：在顶部输入框输入内容，选择分类，点击添加
2. **完成待办**：点击 checkbox 标记完成
3. **编辑待办**：点击编辑按钮，修改内容
4. **删除待办**：点击删除按钮，确认后删除
5. **筛选待办**：按分类或状态筛选
6. **清除已完成**：一键清除所有已完成项
7. **数据持久化**：刷新页面后数据依然存在

---

## 本章小结

本章通过一个完整的待办事项管理系统实战项目，练习了：

1. **项目初始化**：Vite + React 项目创建
2. **状态管理**：Zustand Store 设计
3. **组件开发**：Header、Form、List、Filter 组件
4. **UI 组件**：Ant Design 组件的使用
5. **样式美化**：CSS 响应式设计
6. **数据持久化**：localStorage 集成

---

## 练习与扩展

1. **增加搜索功能**：添加搜索框，按关键字筛选
2. **增加拖拽排序**：使用 react-beautiful-dnd 实现拖拽排序
3. **增加日期提醒**：添加截止日期和提醒功能
4. **增加数据统计**：添加图表展示完成率等统计
5. **部署上线**：将项目部署到 Vercel 或 Netlify

---

## 恭喜完成！

通过本教程的学习，你已经掌握了：

- ✅ React 19 函数式组件开发
- ✅ React Hooks 的使用
- ✅ React 19 新特性
- ✅ Zustand 状态管理
- ✅ Ant Design 组件库
- ✅ 实战项目开发

希望本教程对你学习 React 有所帮助！
