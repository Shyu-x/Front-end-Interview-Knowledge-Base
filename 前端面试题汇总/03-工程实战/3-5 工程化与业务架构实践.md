# 前端工程化与业务架构实践

> 作者: Shyu
> 本文档涵盖 TOB/TOC 业务架构、设计模式、代码质量、设计理论与验收要求

---

## 一、TOB 与 TOC 业务特点与架构差异

### 1.1 TOB（To Business）业务特点

TOB（面向企业）业务是指为企业客户提供产品或服务的业务模式。其特点如下：

| 特性 | 说明 |
|------|------|
| **用户群体** | 企业员工、管理者、决策者 |
| **使用场景** | 办公、管理、数据分析 |
| **功能复杂度** | 高，功能繁多且相互关联 |
| **交互方式** | 鼠标操作为主，操作密集型 |
| **性能要求** | 强调数据处理能力和稳定性 |
| **安全性** | 高，涉及企业敏感数据 |
| **定制化** | 高，需要支持客户化配置 |
| **上线周期** | 长，测试严格 |

**典型 TOB 产品：**
- 企业管理系统（ERP、CRM、OA）
- 财务系统
- 人力资源管理系统
- 数据分析平台
- SaaS 产品

**TOB 前端架构要点：**

```typescript
// TOB 业务特点的架构设计

// 1. 复杂表单处理
interface FormConfig {
  fields: FormField[];
  layout: 'single' | 'double' | 'triple';
  validation: ValidationRules;
  dependencies: FieldDependency[]; // 字段依赖关系
}

// 2. 表格数据处理
interface DataTableConfig {
  columns: TableColumn[];
  pagination: PaginationConfig;
  sorting: SortingConfig;
  filtering: FilteringConfig;
  virtualScroll: boolean; // 大数据虚拟滚动
  export: ExportConfig;
}

// 3. 权限控制系统
interface PermissionSystem {
  resources: Resource[];
  actions: Action[];
  roles: Role[];
  grants: Grant[];
  // 细粒度权限控制
  checkPermission: (resource: string, action: string) => boolean;
}
```

### 1.2 TOC（To Consumer）业务特点

TOC（面向消费者）业务是指为个人用户提供产品或服务的业务模式。其特点如下：

| 特性 | 说明 |
|------|------|
| **用户群体** | 普通消费者 |
| **使用场景** | 娱乐、社交、购物、信息获取 |
| **功能复杂度** | 中等，核心功能突出 |
| **交互方式** | 触摸、滑动、多媒体 |
| **性能要求** | 强调首屏加载和响应速度 |
| **安全性** | 中等，侧重账户安全 |
| **定制化** | 低，标准化体验 |
| **上线周期** | 短，快速迭代 |

**典型 TOC 产品：**
- 电商平台
- 社交应用
- 短视频平台
- 新闻资讯应用
- 在线教育平台

**TOC 前端架构要点：**

```typescript
// TOC 业务特点的架构设计

// 1. 首屏性能优化
interface PerformanceConfig {
  criticalCSS: string[]; // 关键 CSS
  preloadResources: Resource[]; // 预加载资源
  lazyLoadModules: string[]; // 懒加载模块
  skeleton: SkeletonConfig; // 骨架屏配置
  hydration: HydrationConfig; // SSR 水合配置
}

// 2. 体验优化
interface ExperienceConfig {
  gestures: GestureConfig; // 手势配置
  animations: AnimationConfig; // 动画配置
  feedback: FeedbackConfig; // 用户反馈
  offline: OfflineConfig; // 离线体验
}

// 3. A/B 测试
interface ABTestConfig {
  experiments: Experiment[];
  variants: Variant[];
  tracking: TrackingConfig;
  // 流量分配
  allocateTraffic: (experimentId: string, userId: string) => string;
}
```

### 1.3 TOB vs TOC 架构对比

| 维度 | TOB | TOC |
|------|-----|-----|
| **路由复杂度** | 多级嵌套，简单层级深 | 扁平化，层级少 |
| **状态管理** | 复杂表单状态，多模块联动 | 相对简单，用户状态为主 |
| **数据表格** | 核心组件，高性能要求 | 较少使用 |
| **表单处理** | 核心，大量复杂校验 | 较少，登录注册为主 |
| **缓存策略** | 服务端缓存为主 | 客户端缓存重要 |
| **离线能力** | 非必须 | 重要 |
| **国际化** | 重要，多语言多时区 | 相对简单 |
| **主题定制** | 重要，支持品牌定制 | 固定主题 |

---

## 二、设计模式在前端中的应用

### 2.1 创建型模式

#### 2.1.1 工厂模式（Factory Pattern）

```typescript
// 工厂模式 - 创建不同类型的表单组件
interface FormElement {
  render(): HTMLElement;
  validate(): boolean;
  getValue(): any;
}

class InputElement implements FormElement {
  constructor(private config: InputConfig) {}

  render(): HTMLElement {
    const input = document.createElement('input');
    input.type = this.config.type;
    input.placeholder = this.config.placeholder;
    return input;
  }

  validate(): boolean {
    return this.config.required ? !!this.getValue() : true;
  }

  getValue(): any {
    return (document.getElementById(this.config.id) as HTMLInputElement).value;
  }
}

class SelectElement implements FormElement {
  constructor(private config: SelectConfig) {}

  render(): HTMLElement {
    const select = document.createElement('select');
    this.config.options.forEach(opt => {
      const option = document.createElement('option');
      option.value = opt.value;
      option.textContent = opt.label;
      select.appendChild(option);
    });
    return select;
  }

  validate(): boolean {
    return this.config.required ? !!this.getValue() : true;
  }

  getValue(): any {
    return (document.getElementById(this.config.id) as HTMLSelectElement).value;
  }
}

// 工厂类
class FormElementFactory {
  static create(type: 'input' | 'select' | 'textarea' | 'checkbox', config: any): FormElement {
    switch (type) {
      case 'input':
        return new InputElement(config);
      case 'select':
        return new SelectElement(config);
      // ... 其他类型
      default:
        throw new Error(`Unknown element type: ${type}`);
    }
  }
}

// 使用
const form = FormElementFactory.create('input', {
  id: 'username',
  type: 'text',
  placeholder: '请输入用户名',
  required: true
});
```

#### 2.1.2 单例模式（Singleton Pattern）

```typescript
// 单例模式 - 全局状态管理
class GlobalState {
  private static instance: GlobalState;
  private state: Map<string, any> = new Map();
  private listeners: Map<string, Set<Function>> = new Map();

  private constructor() {}

  static getInstance(): GlobalState {
    if (!GlobalState.instance) {
      GlobalState.instance = new GlobalState();
    }
    return GlobalState.instance;
  }

  get(key: string): any {
    return this.state.get(key);
  }

  set(key: string, value: any): void {
    this.state.set(key, value);
    this.notify(key, value);
  }

  subscribe(key: string, listener: Function): () => void {
    if (!this.listeners.has(key)) {
      this.listeners.set(key, new Set());
    }
    this.listeners.get(key)!.add(listener);

    // 返回取消订阅函数
    return () => {
      this.listeners.get(key)?.delete(listener);
    };
  }

  private notify(key: string, value: any): void {
    this.listeners.get(key)?.forEach(listener => listener(value));
  }
}

// 使用
const state = GlobalState.getInstance();
state.set('user', { name: 'Shyu' });
const unsubscribe = state.subscribe('user', (user) => {
  console.log('User updated:', user);
});
```

#### 2.1.3 建造者模式（Builder Pattern）

```typescript
// 建造者模式 - 复杂配置对象构建
interface ChartConfig {
  type: string;
  width: number;
  height: number;
  colors: string[];
  axes: AxesConfig;
  legends: LegendConfig;
  animations: AnimationConfig;
}

class ChartConfigBuilder {
  private config: Partial<ChartConfig> = {};

  setType(type: string): this {
    this.config.type = type;
    return this;
  }

  setSize(width: number, height: number): this {
    this.config.width = width;
    this.config.height = height;
    return this;
  }

  setColors(...colors: string[]): this {
    this.config.colors = colors;
    return this;
  }

  setAxes(axes: AxesConfig): this {
    this.config.axes = axes;
    return this;
  }

  setLegends(legends: LegendConfig): this {
    this.config.legends = legends;
    return this;
  }

  setAnimations(animations: AnimationConfig): this {
    this.config.animations = animations;
    return this;
  }

  build(): ChartConfig {
    return {
      type: 'line',
      width: 800,
      height: 400,
      colors: ['#1890ff', '#52c41a'],
      axes: { x: {}, y: {} },
      legends: { position: 'top' },
      animations: { duration: 300 },
      ...this.config
    } as ChartConfig;
  }
}

// 使用
const chartConfig = new ChartConfigBuilder()
  .setType('line')
  .setSize(1200, 600)
  .setColors('#1890ff', '#52c41a', '#faad14')
  .setAxes({ x: { title: 'X轴' }, y: { title: 'Y轴' } })
  .build();
```

### 2.2 结构型模式

#### 2.2.1 装饰器模式（Decorator Pattern）

```typescript
// 装饰器模式 - 为组件增强功能
class Component {
  render(): string {
    return '<div>基础组件</div>';
  }
}

// 装饰器基类
class ComponentDecorator {
  constructor(protected component: Component) {}

  render(): string {
    return this.component.render();
  }
}

// 具体装饰器
class BorderDecorator extends ComponentDecorator {
  render(): string {
    return `<div style="border: 1px solid #ddd">${this.component.render()}</div>`;
  }
}

class PaddingDecorator extends ComponentDecorator {
  render(): string {
    return `<div style="padding: 16px">${this.component.render()}</div>`;
  }
}

class ShadowDecorator extends ComponentDecorator {
  render(): string {
    return `<div style="box-shadow: 0 2px 8px rgba(0,0,0,0.1)">${this.component.render()}</div>`;
  }
}

// 使用 - 组合多个装饰器
let component = new Component();
component = new BorderDecorator(component);
component = new PaddingDecorator(component);
component = new ShadowDecorator(component);

console.log(component.render());
// 输出: <div style="border: 1px solid #ddd"><div style="padding: 16px"><div style="box-shadow: 0 2px 8px rgba(0,0,0,0.1)"><div>基础组件</div></div></div></div>
```

#### 2.2.2 代理模式（Proxy Pattern）

```typescript
// 代理模式 - 权限控制、数据懒加载

// 真实主题
class UserService {
  getUser(id: string): User {
    // 实际数据库查询
    return { id, name: '用户' + id };
  }

  deleteUser(id: string): void {
    console.log(`删除用户: ${id}`);
  }
}

// 代理类 - 添加权限控制
class UserServiceProxy {
  private realService: UserService;
  private permissions: Set<string>;

  constructor(permissions: Set<string>) {
    this.realService = new UserService();
    this.permissions = permissions;
  }

  getUser(id: string): User | null {
    if (!this.permissions.has('user:read')) {
      console.log('无权限读取用户');
      return null;
    }
    return this.realService.getUser(id);
  }

  deleteUser(id: string): boolean {
    if (!this.permissions.has('user:delete')) {
      console.log('无权限删除用户');
      return false;
    }
    this.realService.deleteUser(id);
    return true;
  }
}

// 使用
const proxy = new UserServiceProxy(new Set(['user:read']));
proxy.getUser('1'); // 有权限，正常返回
proxy.deleteUser('1'); // 无权限，被拒绝
```

### 2.3 行为型模式

#### 2.3.1 观察者模式（Observer Pattern）

```typescript
// 观察者模式 - 事件系统、状态监听

interface Observer {
  update(data: any): void;
}

interface Subject {
  attach(observer: Observer): void;
  detach(observer: Observer): void;
  notify(): void;
}

class EventEmitter implements Subject {
  private observers: Map<string, Set<Observer>> = new Map();

  attach(event: string, observer: Observer): void {
    if (!this.observers.has(event)) {
      this.observers.set(event, new Set());
    }
    this.observers.get(event)!.add(observer);
  }

  detach(event: string, observer: Observer): void {
    this.observers.get(event)?.delete(observer);
  }

  emit(event: string, data: any): void {
    this.observers.get(event)?.forEach(observer => observer.update(data));
  }

  notify(): void {}
}

// 使用
class UserObserver implements Observer {
  update(data: any): void {
    console.log('用户数据更新:', data);
  }
}

const emitter = new EventEmitter();
const observer = new UserObserver();

emitter.attach('user:update', observer);
emitter.emit('user:update', { name: 'Shyu' });
```

#### 2.3.2 策略模式（Strategy Pattern）

```typescript
// 策略模式 - 多种算法/校验规则切换

interface ValidationStrategy {
  validate(value: any): boolean;
  getErrorMessage(): string;
}

// 策略实现
class EmailValidation implements ValidationStrategy {
  validate(value: any): boolean {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
  }

  getErrorMessage(): string {
    return '请输入有效的邮箱地址';
  }
}

class PhoneValidation implements ValidationStrategy {
  validate(value: any): boolean {
    return /^1[3-9]\d{9}$/.test(value);
  }

  getErrorMessage(): string {
    return '请输入有效的手机号码';
  }
}

class LengthValidation implements ValidationStrategy {
  constructor(private min: number, private max: number) {}

  validate(value: any): boolean {
    return value.length >= this.min && value.length <= this.max;
  }

  getErrorMessage(): string {
    return `长度必须在 ${this.min}-${this.max} 之间`;
  }
}

// 策略上下文
class FormField {
  private strategy: ValidationStrategy;

  constructor(private name: string, strategy: ValidationStrategy) {
    this.strategy = strategy;
  }

  setStrategy(strategy: ValidationStrategy): void {
    this.strategy = strategy;
  }

  validate(value: any): { valid: boolean; message: string } {
    const valid = this.strategy.validate(value);
    return {
      valid,
      message: valid ? '' : this.strategy.getErrorMessage()
    };
  }
}

// 使用
const emailField = new FormField('email', new EmailValidation());
console.log(emailField.validate('test@example.com')); // { valid: true, message: '' }
console.log(emailField.validate('invalid')); // { valid: false, message: '请输入有效的邮箱地址' }

// 切换策略
emailField.setStrategy(new LengthValidation(5, 50));
console.log(emailField.validate('abc')); // { valid: false, message: '长度必须在 5-50 之间' }
```

#### 2.3.3 命令模式（Command Pattern）

```typescript
// 命令模式 - 撤销/重做、操作记录

interface Command {
  execute(): void;
  undo(): void;
}

class CommandManager {
  private history: Command[] = [];
  private redoStack: Command[] = [];

  execute(command: Command): void {
    command.execute();
    this.history.push(command);
    this.redoStack = []; // 新命令清除重做栈
  }

  undo(): void {
    const command = this.history.pop();
    if (command) {
      command.undo();
      this.redoStack.push(command);
    }
  }

  redo(): void {
    const command = this.redoStack.pop();
    if (command) {
      command.execute();
      this.history.push(command);
    }
  }
}

// 具体命令
class AddItemCommand implements Command {
  constructor(
    private list: string[],
    private item: string
  ) {}

  execute(): void {
    this.list.push(this.item);
    console.log(`添加: ${this.item}`);
  }

  undo(): void {
    const index = this.list.indexOf(this.item);
    if (index > -1) {
      this.list.splice(index, 1);
      console.log(`撤销添加: ${this.item}`);
    }
  }
}

class RemoveItemCommand implements Command {
  private removedItem: string = '';

  constructor(
    private list: string[],
    private item: string
  ) {}

  execute(): void {
    const index = this.list.indexOf(this.item);
    if (index > -1) {
      this.removedItem = this.list.splice(index, 1)[0];
      console.log(`删除: ${this.removedItem}`);
    }
  }

  undo(): void {
    if (this.removedItem) {
      this.list.push(this.removedItem);
      console.log(`撤销删除: ${this.removedItem}`);
    }
  }
}

// 使用
const list: string[] = [];
const manager = new CommandManager();

manager.execute(new AddItemCommand(list, '项目A'));
manager.execute(new AddItemCommand(list, '项目B'));
manager.execute(new RemoveItemCommand(list, '项目A'));

console.log('当前列表:', list); // ['项目B']

manager.undo();
console.log('撤销后:', list); // ['项目A', '项目B']

manager.redo();
console.log('重做后:', list); // ['项目B']
```

---

## 三、代码质量体系

### 3.1 代码质量维度

```typescript
// 代码质量评估维度

interface CodeQualityMetrics {
  // 可维护性
  maintainability: {
    cyclomaticComplexity: number; // 圈复杂度
    linesOfCode: number; // 代码行数
    coupling: number; // 耦合度
    cohesion: number; // 内聚度
  };

  // 可读性
  readability: {
    namingQuality: number; // 命名质量
    commentRatio: number; // 注释比例
    functionLength: number; // 函数长度
  };

  // 可测试性
  testability: {
    branchCoverage: number; // 分支覆盖率
    mockability: number; // 可mock程度
  };

  // 性能
  performance: {
    bundleSize: number; // 包大小
    loadTime: number; // 加载时间
    runtimePerformance: number; // 运行时性能
  };

  // 安全性
  security: {
    vulnerabilityCount: number; // 漏洞数量
    sensitiveDataExposure: boolean; // 敏感数据暴露
  };
}
```

### 3.2 ESLint 规则配置

```javascript
// .eslintrc.js - 团队代码规范配置

module.exports = {
  root: true,
  env: {
    browser: true,
    es2021: true,
    node: true,
  },
  extends: [
    'eslint:recommended',
    'plugin:react/recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  parser: '@typescript-eslint/parser',
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module',
    ecmaFeatures: {
      jsx: true,
    },
  },
  plugins: ['react', '@typescript-eslint', 'react-hooks'],
  rules: {
    // 强制使用 const
    'prefer-const': 'error',

    // 禁止 var
    'no-var': 'error',

    // 强制使用 === 而不是 ==
    'eqeqeq': ['error', 'always'],

    // 限制函数最大参数数量
    'max-params': ['warn', 3],

    // 限制函数最大复杂度
    'complexity': ['warn', 10],

    // 强制使用有意义的变量名
    'camelcase': 'error',

    // 禁止未使用的变量
    'no-unused-vars': 'error',

    // 强制使用箭头函数
    'prefer-arrow-callback': 'warn',

    // React 相关
    'react/react-in-jsx-scope': 'off',
    'react/prop-types': 'off',
    'react-hooks/rules-of-hooks': 'error',
    'react-hooks/exhaustive-deps': 'warn',

    // TypeScript 相关
    '@typescript-eslint/no-explicit-any': 'warn',
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
  settings: {
    react: {
      version: 'detect',
    },
  },
};
```

### 3.3 Prettier 配置

```javascript
// .prettierrc.js

module.exports = {
  // 打印宽度
  printWidth: 100,

  // 使用 2 个空格缩进
  tabWidth: 2,

  // 使用空格而不是 Tab
  useTabs: false,

  // 语句末尾不加分号
  semi: false,

  // 使用单引号
  singleQuote: true,

  // 对象属性引号
  quoteProps: 'as-needed',

  // 尾随逗号
  trailingComma: 'es5',

  // 大括号内的首尾空格
  bracketSpacing: true,

  // JSX 标签闭合符不换行
  jsxBracketSameLine: false,

  // 箭头函数括号
  arrowParens: 'avoid',

  // 范围格式
  rangeStart: 0,
  rangeEnd: Infinity,

  // 嵌入 printWidth 的方式
  embeddedLanguageFormatting: 'auto',
};
```

### 3.4 Git 提交规范

```bash
# Git 提交信息格式
# <type>(<scope>): <subject>
#
# <body>
#
# <footer>

# Type 类型
feat:     # 新功能
fix:      # Bug 修复
docs:     # 文档更新
style:    # 代码格式（不影响功能）
refactor: # 重构（既不是新功能也不是修复）
perf:     # 性能优化
test:     # 测试相关
chore:    # 构建过程或辅助工具变动

# 示例
feat(form): 添加表单验证功能

新增邮箱、手机号、身份证等常用表单验证规则
支持自定义验证规则和错误提示

Closes #123
```

---

## 四、设计理论与原则

### 4.1 SOLID 原则

| 原则 | 说明 | 违反后果 |
|------|------|----------|
| **S 单一职责** | 每个类/函数只负责一项功能 | 难以维护、难以测试 |
| **O 开闭原则** | 对扩展开放，对修改关闭 | 频繁修改影响稳定性 |
| **L 里氏替换** | 子类能替换父类 | 破坏继承体系 |
| **I 接口隔离** | 使用多个专用接口 | 依赖冗余 |
| **D 依赖倒置** | 依赖抽象而非具体 | 难以替换实现 |

```typescript
// 单一职责示例
// 违反：同时处理数据获取和UI渲染
class UserCard extends Component {
  async loadUser(id: string) {
    const response = await fetch(`/api/users/${id}`);
    const user = await response.json();
    this.render(user);
  }

  render(user: User) {
    // 渲染逻辑
  }
}

// 正确：分离关注点
class UserService {
  async getUser(id: string): Promise<User> {
    const response = await fetch(`/api/users/${id}`);
    return response.json();
  }

class UserCard extends Component {
  constructor(private userService: UserService) {}

  async loadUser(id: string) {
    const user = await this.userService.getUser(id);
    this.render(user);
  }

  render(user: User) {
    // 渲染逻辑
  }
}
```

### 4.2 DRY / KISS / YAGNI

| 原则 | 说明 | 适用场景 |
|------|------|----------|
| **DRY** | Don't Repeat Yourself - 不要重复 | 重复代码提取 |
| **KISS** | Keep It Simple, Stupid - 保持简单 | 避免过度设计 |
| **YAGNI** | You Aren't Gonna Need It - 暂不需要 | 不做未来预留 |

### 4.3 设计系统

```typescript
// 设计系统 - Design System

// 1. 基础设计 Token
interface DesignTokens {
  colors: {
    primary: string;
    secondary: string;
    success: string;
    warning: string;
    error: string;
    background: string;
    text: string;
  };
  spacing: {
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
  };
  typography: {
    fontFamily: string;
    fontSize: {
      xs: number;
      sm: number;
      md: number;
      lg: number;
      xl: number;
    };
    fontWeight: {
      regular: number;
      medium: number;
      bold: number;
    };
  };
  borderRadius: {
    sm: number;
    md: number;
    lg: number;
    full: number;
  };
  shadows: {
    sm: string;
    md: string;
    lg: string;
  };
}

// 2. 组件规范
interface ComponentSpecs {
  Button: {
    sizes: ('sm' | 'md' | 'lg')[];
    variants: ('primary' | 'secondary' | 'ghost' | 'danger')[];
    states: ('default' | 'hover' | 'active' | 'disabled' | 'loading')[];
  };
  Input: {
    sizes: ('sm' | 'md' | 'lg')[];
    states: ('default' | 'focus' | 'error' | 'disabled')[];
  };
}

// 3. 响应式断点
interface Breakpoints {
  xs: number;  // 480px
  sm: number;  // 768px
  md: number;  // 1024px
  lg: number;  // 1280px
  xl: number;  // 1536px
}
```

---

## 五、验收标准与质量门禁

### 5.1 代码审查 Checklist

```markdown
## Code Review 检查清单

### 功能性
- [ ] 代码实现是否符合需求
- [ ] 边界情况是否处理
- [ ] 错误处理是否完善
- [ ] 单元测试是否覆盖

### 可读性
- [ ] 变量/函数命名是否清晰
- [ ] 注释是否充分且必要
- [ ] 函数长度是否合理 (< 50 行)
- [ ] 嵌套层级是否过深 (< 3 层)

### 可维护性
- [ ] 是否有重复代码
- [ ] 是否遵循 DRY 原则
- [ ] 模块耦合是否合理
- [ ] 是否有硬编码

### 性能
- [ ] 是否有不必要的重渲染
- [ ] 是否有内存泄漏风险
- [ ] 大数据是否虚拟滚动
- [ ] 图片是否懒加载

### 安全性
- [ ] 是否有 XSS 风险
- [ ] 是否有敏感数据泄露
- [ ] 权限控制是否正确

### 兼容性
- [ ] 浏览器兼容性
- [ ] 响应式适配
- [ ] 无障碍访问 (a11y)
```

### 5.2 发布前检查

```yaml
# 发布前质量门禁

quality_gates:
  # 代码规范
  lint:
    enabled: true
    fail_on_error: true
    max_warnings: 0

  # 测试覆盖
  coverage:
    min_branch_coverage: 80
    min_function_coverage: 80
    min_line_coverage: 80

  # 安全扫描
  security:
    enabled: true
    fail_on_vulnerability: true
    allowed_severity: medium

  # 性能
  performance:
    bundle_size_limit: 500KB
    first_contentful_paint: < 1.5s
    largest_contentful_paint: < 2.5s
    time_to_interactive: < 3s

  # 浏览器兼容性
  browser_compatibility:
    chrome: last 2 versions
    firefox: last 2 versions
    safari: last 2 versions
    edge: last 2 versions
    ie: false

  # 无障碍
  accessibility:
    enabled: true
    min_axe_score: 90
```

### 5.3 TOB 项目验收标准

```markdown
# TOB 项目验收标准

## 功能验收
- [ ] 所有需求功能点完成
- [ ] 业务流程跑通
- [ ] 异常流程处理正确
- [ ] 权限控制生效
- [ ] 数据准确性验证

## 性能验收
- [ ] 页面首屏加载 < 2s
- [ ] 表格 1000 条数据渲染 < 1s
- [ ] 复杂表单保存响应 < 500ms
- [ ] 大量数据导出 < 30s

## 稳定性验收
- [ ] 连续运行 72 小时无崩溃
- [ ] 内存无明显泄漏
- [ ] 接口错误有友好提示

## 兼容性验收
- [ ] Chrome/Edge/Firefox 正常
- [ ] 1920x1080 / 1366x768 正常
- [ ] 打印功能正常

## 安全验收
- [ ] XSS 防护有效
- [ ] CSRF token 验证
- [ ] 敏感数据加密传输
- [ ] 越权访问被阻止

## 文档验收
- [ ] 用户操作手册
- [ ] 部署文档
- [ ] API 接口文档
```

### 5.4 TOC 项目验收标准

```markdown
# TOC 项目验收标准

## 体验验收
- [ ] 首屏加载 < 1.5s
- [ ] 骨架屏 300ms 内显示
- [ ] 动画流畅 60fps
- [ ] 滑动操作无卡顿

## 响应式验收
- [ ] iPhone / Android 正常
- [ ] iPad 平板适配
- [ ] 刘海屏/全面屏适配

## 弱网验收
- [ ] 3G 网络可用
- [ ] 离线缓存生效
- [ ] 请求超时处理

## 埋点验收
- [ ] 关键事件埋点
- [ ] 性能监控
- [ ] 错误监控

## A/B 测试验收
- [ ] 流量分配正确
- [ ] 数据隔离正确
- [ ] 切换无感知
```

---

## 六、业务架构最佳实践

### 6.1 TOB 业务架构

```
┌─────────────────────────────────────────────────────────┐
│                    TOB 业务架构                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   表现层                         │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 管理后台 │ │ 用户门户 │ │ 移动端 H5   │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   业务层                         │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 表单引擎 │ │ 表格引擎 │ │ 工作流引擎  │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 权限引擎 │ │ 报表引擎 │ │ 消息中心    │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   基础设施层                     │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 状态管理 │ │ API网关  │ │ 缓存层      │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.2 TOC 业务架构

```
┌─────────────────────────────────────────────────────────┐
│                    TOC 业务架构                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   体验优化层                      │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 骨架屏  │ │ 预加载   │ │ 离线缓存    │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 动画库  │ │ 手势库   │ │ 媒体处理    │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   业务组件层                      │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 商品卡片 │ │ 购物车  │ │ 订单流程    │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 直播组件│ │ 聊天组件 │ │ 评论组件    │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │                   数据与策略层                    │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │ 推荐系统 │ │ A/B测试  │ │ 埋点分析    │   │   │
│  │  └─────────┘ └─────────┘ └─────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

> 作者: Shyu
> 最后更新: 2026-03-04
